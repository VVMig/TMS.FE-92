export enum Routes {
  HOME = "/",
  REGISTER = "/register",
  CONFIRMATION_EMAIL = "/confirm",
  LOGIN = "/login",
  ALL_POSTS = "/posts",
  POST = "/posts/:id",
  VERIFICATION_EMAIL = "/activate/:uid/:token",
}
